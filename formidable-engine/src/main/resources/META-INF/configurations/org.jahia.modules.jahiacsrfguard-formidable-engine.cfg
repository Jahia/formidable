# Keep guest bypass unchanged: CSRFGuard remains relevant only for authenticated users.
# Add the Formidable submit servlet path to the protected URL patterns for this module.
resolvedUrlPatterns = *.do,/modules/formidable-engine/form-submit
