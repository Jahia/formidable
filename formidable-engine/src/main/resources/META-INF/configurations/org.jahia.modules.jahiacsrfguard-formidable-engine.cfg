# Keep guest bypass unchanged: CSRFGuard remains relevant only for authenticated users.
# Protect the Formidable submit endpoint for authenticated users.
urlPatterns = /modules/formidable-engine/form-submit
