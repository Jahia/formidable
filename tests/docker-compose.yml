services:
  jahia:
    image: '${JAHIA_IMAGE}'
    container_name: jahia
    ports:
      - '8000:8000'
      - '8080:8080'
      - '8101:8101'
    environment:
      JAHIA_LICENSE: ${JAHIA_LICENSE}
      SUPER_USER_PASSWORD: ${SUPER_USER_PASSWORD}
      CATALINA_OPTS: ${CATALINA_OPTS}
      NEXUS_USERNAME: ${NEXUS_USERNAME}
      NEXUS_PASSWORD: ${NEXUS_PASSWORD}
      JPDA: 'true'
    extra_hosts:
      - jahia:127.0.0.1
    networks:
      - stack
  # Cypress container
  cypress:
    image: '${TESTS_IMAGE}'
    ipc: host
    container_name: cypress
    depends_on:
      - jahia
    environment:
      MANIFEST: ${MANIFEST}
      JAHIA_URL: ${JAHIA_URL}
      JAHIA_HOST: ${JAHIA_HOST}
      JAHIA_PORT: ${JAHIA_PORT}
      NEXUS_USERNAME: ${NEXUS_USERNAME}
      NEXUS_PASSWORD: ${NEXUS_PASSWORD}
      SUPER_USER_PASSWORD: ${SUPER_USER_PASSWORD}
    networks:
      - stack
  # SMTP catcher for the email form actions. Point Jahia's mail server settings at
  # mailpit:1025 (Administration > Configuration > Mail server) and browse received
  # messages on http://localhost:8025 (REST API available for e2e assertions).
  mailpit:
    image: 'axllent/mailpit:latest'
    container_name: mailpit
    ports:
      - '8025:8025'
      - '1025:1025'
    networks:
      - stack
networks:
  stack:
