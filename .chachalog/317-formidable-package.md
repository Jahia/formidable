---
# Allowed version bumps: patch, minor, major
formidable: minor
---

Added a published package so a module of your own renders form fields like the built-in ones, masks included (#317)

Add `@jahia/formidable` to your module (`yarn add @jahia/formidable`) to import the help block, the validation-message attributes and the input mask instead of copying their markup.
